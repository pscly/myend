version: '3'
services:
  my_end_py:
    build: 
      context: ./
      dockerfile: Dockerfile
    container_name: flask_myend_${NODETYPE}
    command: sh run.sh
    ports:
      - "31002:31001"
    volumes:
      - ./:/py_end
